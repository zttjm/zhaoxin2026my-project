# AI Agent 使用记录

## 使用的工具

- **Claude Code**
- **DeepSeek**

## 任务1

1. 根据 B 站教程，下载并安装了 **Node.js**。
2. 在管理员身份下运行 **PowerShell**，使用 npm 下载安装了 **Claude Code**。
3. 在 GitHub 上下载了 **cc-switch**。
4. 在 DeepSeek 的 API 开发平台申请了自己的 **API Key**，并在 cc-switch 中配置使用。
5. 我让claude阅读了这个项目仓库，在README.md将Aiagent.md的简要内容和查看路径写入.
6. claude在README.md中添加了一个三级标题AIagent 工具，使用加粗字体记录了使用的aiagent工具，写入了查看路径
7. 使用`git diff`我看到了文件的修改，文件内容的增加和删减都能看出
8. 结果符合预期。

## 任务2

1. 普通聊天 AI 只能根据用户的输入生成文字，不能完成修改文件等项目。Agent 除了聊天能力，还接入了**工具（Tool）和执行环境（终端、文件系统）**。它本身并不直接碰文件，而是通过"工具调用"表达意图——例如"我要读这个文件"，由外部程序真正去执行，再把结果拿回来继续思考。所以 Agent = 大模型 + 工具 + 执行环境。
2. Tool 是 Agent 的**手脚**。它把模型的"语言能力"转化为"真实世界的操作"：读文件、搜索、执行命令、编辑代码等。模型负责判断"该用哪个工具、传什么参数"工具负责真正执行，结果回传给模型，形成 **思考 → 行动 → 观察 → 再思考** 的循环。
3. 因为 Agent 会自主做大量操作，没有边界约束就可能做出危险或不合规的行为：误删文件、执行危险命令、泄露敏感信息等。规则就像员工手册，告诉它**什么能做、什么不能做、按什么风格干活、不确定时先问人**，从而降低风险、保证输出一致。
4. 模型有**上下文窗口（context window）长度上限**。对话内容、工具调用结果都会不断塞进上下文，一旦超过上限，最早的内容会被截断或压缩，看起来就像"忘记"了。所以上下文越长、工具调用越多，越容易"忘"。额度按 **token** 计算。输入的内容 + 历史上下文工具结果都算**输入 token**，它生成的内容算**输出 token**，两者合计计费。中文大约 1 个汉字 ≈ 1~2 个 token。上下文越长、来回越多，消耗越快。
5. 风险包括：误删或覆盖重要文件（如 `rm -rf`）、执行破坏性命令、把密钥/源码等敏感数据上传到外部、被**提示注入（prompt injection）**诱导执行危险操作等。
