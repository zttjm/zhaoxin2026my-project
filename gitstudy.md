# Git 学习过程记录

### AI 使用说明：本部分使用claude code协助完成代码实现，最终修改由本人通过 git diff 检查。

## 环境准备

- 根据教程安装了 Git
- 创建了 GitHub 账号

## 已掌握的命令

### 1. `git add`

把工作区的改动暂存到暂存区。

```bash
git add <文件名>    # 暂存指定文件
git add .          # 暂存当前目录所有改动
```

### 2. `git commit`

把暂存区的内容提交到本地仓库。

```bash
git commit -m "提交说明"
```

### 3. `git clone`

从远程仓库克隆一个副本到本地。

```bash
git clone <仓库地址>
```

### 4. `git push`

把本地提交推送到远程仓库。

```bash
git push
```

## 学习时间线

| 步骤 | 内容              | 状态    |
| ---- | ----------------- | ------- |
| 1    | 安装 Git          | ✅ 完成 |
| 2    | 创建 GitHub 账号  | ✅ 完成 |
| 3    | 练习 `git add`    | ✅ 完成 |
| 4    | 练习 `git commit` | ✅ 完成 |
| 5    | 练习 `git clone`  | ✅ 完成 |
| 6    | 练习 `git push`   | ✅ 完成 |
